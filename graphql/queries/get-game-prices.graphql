query GetGamePrices($gameId: String!) {
  getGamePrices(gameId: $gameId) {
    gameId
    title
    thumb
    cheapestPrice
    cheapestEverPrice
    cheapestEverDate
    stores {
      storeId
      storeName
      price
      retailPrice
      savings
      dealUrl
      storeIcon
    }
  }
}
